# ADR-000：决策记录模板

> 复制本文件为 `ADR-XXX-<短横线小写标题>.md`，并把「状态」改为 `已定稿`。一个决策一份文件；**不要**把结论散落在任务卡里。

- **编号**：ADR-000
- **状态**：模板 / 待定 / 已定稿 / 已废弃（被 ADR-XXX 取代）
- **日期**：YYYY-MM-DD
- **决策者**：DSH（+ 用户确认，若涉及产品取舍）
- **相关**：任务卡 T0x、`docs/ARCHITECTURE.md` §x

## 背景

要解决什么问题，为什么现在必须定。写清约束来自哪里（平台限制 / 真机实测 / 用户选择），给出可点击的来源。

## 备选方案

| 方案 | 做法 | 优点 | 缺点 | 实测结果 |
|---|---|---|---|---|
| A | | | | |
| B | | | | |
| C | | | | |

## 决策

选 **X**。一句话说清选它的核心理由。

## 后果

- 正面：
- 负面（接受的代价）：
- 需要同步修改的地方：

## 实测证据（必需）

真机型号 / 系统版本、执行命令、关键输出、错误码原文。**没有实测证据的决策不算定稿。**
